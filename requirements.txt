# 数据库查询工具依赖

# SSH连接
paramiko>=2.12.0

# TOML配置文件解析
toml>=0.10.2

# 注意：tkinter 是 Python 标准库，但在 Linux 上需要安装系统包
# Ubuntu/Debian: sudo apt-get install python3-tk
# CentOS/RHEL:   sudo yum install python3-tkinter
# 如果不使用图形界面，可以跳过此步骤

# 可选：如果需要更好的SSH体验
# fabric>=3.0.0
